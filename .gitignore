node_modules/
dist/
.output/
.nitro/
.nuxt/
.cache/
coverage/
.playwright/
playwright-report/
test-results/
.DS_Store
*.log
.vscode/
.idea/
.env
.env.local
.turbo/

# Litro: generated files (overwritten on every build; contain machine-local absolute paths)
apps/*/routes.generated.ts
apps/*/server/stubs/

# unstorage filesystem driver runtime data (registered OAuth apps + state).
# Contains real client_secret values from local dev sign-ins — never commit.
apps/*/.data/

# Build-time metadata: regenerated by scripts/write-build-meta.mjs before
# every dev/build/test/typecheck; checking it in would dirty git on every
# HEAD change. Anything running typecheck or tests goes through pnpm scripts
# which run write-build-meta first.
apps/*/server/build-meta.generated.ts

# Design-token CSS inlined at build time by scripts/write-tokens-head.mjs —
# must be baked into the server bundle because the Docker runtime image
# ships only `dist/` (no node_modules to resolve the workspace package).
apps/*/server/lib/tokens-head.generated.ts

# UnoCSS-generated utility-class CSS, same pattern as tokens-head.generated.ts.
# Regenerated on every `pnpm build-meta` by scripts/write-uno-head.mjs.
apps/*/server/lib/uno-head.generated.ts
