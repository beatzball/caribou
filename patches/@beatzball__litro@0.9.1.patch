diff --git a/dist/adapter/elena/index.js b/dist/adapter/elena/index.js
index 1c90aa6908d442e63371df6c46dd47dc8127729a..34ae9fd9491f916c2ec3225af804c5d06594563f 100644
--- a/dist/adapter/elena/index.js
+++ b/dist/adapter/elena/index.js
@@ -141,6 +141,16 @@ const ENTITIES = {
 function decodeEntities(s) {
     return s.replace(/&(?:amp|lt|gt|quot|#39);/g, m => ENTITIES[m] || m);
 }
+/** Sentinel id used by adoption-suppression contracts in DSD-aware Elena bases. */
+const DSD_SENTINEL_STYLE_ID = 'caribou-dsd-style';
+/**
+ * Flatten a component's `static styles` field to a single string for DSD inlining.
+ */
+function flattenStyles(styles) {
+    if (styles == null) return '';
+    const list = Array.isArray(styles) ? styles : [styles];
+    return list.filter((s) => typeof s === 'string').join('\n');
+}
 /** Regex to find custom element tags (tags containing a hyphen). */
 const CE_TAG_RE = /<([a-z][a-z0-9]*-[a-z0-9-]*)([^>]*)(?:\/>|>([\s\S]*?)<\/\1>)/gi;
 /** Parse attributes from an HTML attribute string, decoding HTML entities. */
@@ -163,23 +173,33 @@ function parseAttrs(attrStr) {
  */
 function expandNestedCEs(html, ceMap, depth = 0) {
     if (depth > 10)
-        return html; // Guard against infinite recursion.
+        return html;
     return html.replace(CE_TAG_RE, (match, tag, attrStr, childContent) => {
-        // Skip already-expanded CEs (have hydrated attribute from a prior pass).
         if (attrStr && /\bhydrated\b/.test(attrStr))
             return match;
         const ComponentClass = ceMap.get(tag);
         if (!ComponentClass)
-            return match; // Not registered — leave as-is.
-        // First expand any nested CEs in the children (bottom-up).
+            return match;
         const expandedChildren = childContent
             ? expandNestedCEs(childContent, ceMap, depth + 1)
             : undefined;
         const attrs = parseAttrs(attrStr || '');
+        const shadow = ComponentClass.shadow;
+        if (shadow === 'open' || shadow === 'closed') {
+            const innerHTML = renderComponent(ComponentClass, attrs, undefined, undefined);
+            const expandedInner = expandNestedCEs(innerHTML, ceMap, depth + 1);
+            const styles = flattenStyles(ComponentClass.styles);
+            const dsd =
+                `<template shadowrootmode="${shadow}">` +
+                `<style id="${DSD_SENTINEL_STYLE_ID}">${styles}</style>` +
+                expandedInner +
+                `</template>`;
+            const lightChildren = expandedChildren ?? '';
+            return `<${tag}${attrStr || ''} hydrated>${dsd}${lightChildren}</${tag}>`;
+        }
         const innerHTML = renderComponent(ComponentClass, attrs, undefined, expandedChildren);
         if (!innerHTML)
             return match;
-        // Then expand any nested CEs in this component's rendered output.
         const expanded = expandNestedCEs(innerHTML, ceMap, depth + 1);
         return `<${tag}${attrStr || ''} hydrated>${expanded}</${tag}>`;
     });
@@ -205,9 +225,19 @@ async function* renderElenaPage(tag, serverData) {
         throw new Error(`[litro:elena] Component <${tag}> not found in registry. ` +
             `Registered: ${[...ceMap.keys()].join(', ')}`);
     }
+    const shadow = ComponentClass.shadow;
     const innerHTML = renderComponent(ComponentClass, {}, serverData);
-    // Expand any nested custom elements in the render output.
     const expanded = expandNestedCEs(innerHTML, ceMap);
+    if (shadow === 'open' || shadow === 'closed') {
+        const styles = flattenStyles(ComponentClass.styles);
+        const dsd =
+            `<template shadowrootmode="${shadow}">` +
+            `<style id="${DSD_SENTINEL_STYLE_ID}">${styles}</style>` +
+            expanded +
+            `</template>`;
+        yield `<${tag} hydrated>${dsd}</${tag}>`;
+        return;
+    }
     yield `<${tag} hydrated>${expanded}</${tag}>`;
 }
 export const elenaAdapter = {
diff --git a/dist/adapter/elena/runtime/LitroLink.js b/dist/adapter/elena/runtime/LitroLink.js
index b663152868509f5713d6a2e8f6c0b17006333a98..f6f91d4a72b2d2fb5180f86bc7388836b287ffb8 100644
--- a/dist/adapter/elena/runtime/LitroLink.js
+++ b/dist/adapter/elena/runtime/LitroLink.js
@@ -1,42 +1,47 @@
 /**
  * LitroLink (Elena) — <litro-link>
  *
- * Elena implementation of the SPA navigation link. Mirrors the Lit/FAST
- * version's behavior:
+ * Composite SPA-navigation wrapper. Author writes:
+ *   <litro-link><a href="/path">Label</a></litro-link>
  *
- * 1. Light DOM with an inner <a> for progressive enhancement
- * 2. Capture-phase click handler on the host element
- * 3. Intercepts same-origin, modifier-free left clicks on internal paths
- * 4. Dynamic import of litro-router (client-only)
- *
- * Elena uses static props for reactive attributes and render() for the
- * inner <a> element. Since Elena renders to light DOM, the <a> is directly
- * in the document tree — no shadow root.
+ * No render, no shadow, no styles. Capture-phase click handler walks
+ * composedPath to find the inner <a> and routes via LitroRouter when
+ * the anchor is an internal same-origin path with no target / modifier.
  */
-import { Elena, html } from '@elenajs/core';
+import { Elena } from '@elenajs/core';
 export class LitroLink extends Elena(HTMLElement) {
     constructor() {
         super(...arguments);
-        this.href = '';
-        this.target = '';
-        this.rel = '';
         this._clickHandler = (e) => {
-            if (this.target)
+            if (e.button !== 0)
                 return;
             if (e.metaKey || e.ctrlKey || e.shiftKey || e.altKey)
                 return;
-            if (!this.href.startsWith('/'))
+            if (e.defaultPrevented)
+                return;
+            const path = e.composedPath();
+            let anchor;
+            for (const node of path) {
+                if (node === this)
+                    break;
+                if (node instanceof HTMLAnchorElement) {
+                    anchor = node;
+                    break;
+                }
+            }
+            if (!anchor)
+                return;
+            if (anchor.target)
+                return;
+            const href = anchor.getAttribute('href');
+            if (!href || !href.startsWith('/'))
                 return;
             e.preventDefault();
-            void import('@beatzball/litro-router').then(({ LitroRouter }) => LitroRouter.go(this.href));
+            void import('@beatzball/litro-router').then(({ LitroRouter }) => LitroRouter.go(href));
         };
     }
     static { this.tagName = 'litro-link'; }
-    static { this.props = ['href', 'target', 'rel']; }
     connectedCallback() {
-        // Register capture-phase click handler BEFORE super.connectedCallback()
-        // so it is active immediately on SSR'd elements (before Elena's
-        // hydration logic runs).
         this.addEventListener('click', this._clickHandler, true);
         if (typeof super.connectedCallback === 'function') {
             super.connectedCallback();
@@ -48,16 +53,5 @@ export class LitroLink extends Elena(HTMLElement) {
             super.disconnectedCallback();
         }
     }
-    render() {
-        // Elena is light DOM — <slot> has no meaning without shadow DOM.
-        // Use this.text (Elena's built-in property that captures the element's
-        // text content before hydration) to render the link text.
-        return html `<a
-      href="${this.href}"
-      target="${this.target}"
-      rel="${this.rel}"
-    >${this.text}</a>`;
-    }
 }
 LitroLink.define();
-//# sourceMappingURL=LitroLink.js.map
\ No newline at end of file
diff --git a/dist/plugins/path-to-route.js b/dist/plugins/path-to-route.js
index 7cf543105c44ec347dd4cb1d5545d06e5f60dff1..6eaa9bff8ce564633904fc9a8c00bdf8be3e55a2 100644
--- a/dist/plugins/path-to-route.js
+++ b/dist/plugins/path-to-route.js
@@ -33,10 +33,13 @@ function transformSegment(segment) {
     if (optionalMatch) {
         return `:${optionalMatch[1]}?`;
     }
-    // Dynamic: [param] → :param
-    const dynamicMatch = segment.match(/^\[(.+)\]$/);
+    // Dynamic with optional prefix/suffix: pre[param]suf → pre:paramsuf
+    // (Mastodon-style URLs need pages/@[handle]/[statusId].ts — Nuxt and
+    // SvelteKit also allow this.)
+    const dynamicMatch = segment.match(/^([^\[\]]*)\[([^\[\]]+)\]([^\[\]]*)$/);
     if (dynamicMatch) {
-        return `:${dynamicMatch[1]}`;
+        const [, prefix, param, suffix] = dynamicMatch;
+        return `${prefix}:${param}${suffix}`;
     }
     // index → '' (stripped by the caller)
     if (segment === 'index') {
@@ -45,11 +48,11 @@ function transformSegment(segment) {
     return segment;
 }
 /**
- * Returns true if a transformed route segment is dynamic (starts with ':')
- * or is a catch-all (contains '(.*)').
+ * Returns true if a transformed route segment is dynamic — bare `:param`
+ * or prefixed `pre:param` (e.g. `@:handle`).
  */
 function isSegmentDynamic(transformed) {
-    return transformed.startsWith(':');
+    return /:[A-Za-z_]/.test(transformed);
 }
 /**
  * Returns true if a transformed route segment is a catch-all.
diff --git a/src/adapter/elena/index.ts b/src/adapter/elena/index.ts
index 9d1c54a5efbd5056782155e047656e6b19d6a7c3..b088987ce9ae4828e2bb92185d67d7ec3d16ced7 100644
--- a/src/adapter/elena/index.ts
+++ b/src/adapter/elena/index.ts
@@ -143,6 +143,21 @@ function decodeEntities(s: string): string {
   return s.replace(/&(?:amp|lt|gt|quot|#39);/g, m => ENTITIES[m] || m);
 }
 
+/** Sentinel id used by adoption-suppression contracts in DSD-aware Elena bases. */
+const DSD_SENTINEL_STYLE_ID = 'caribou-dsd-style';
+
+/**
+ * Flatten a component's `static styles` field to a single string for DSD inlining.
+ * Accepts string | string[] | (string | CSSStyleSheet)[]. CSSStyleSheet entries
+ * are skipped — no constructable-stylesheet platform on the SSR side. Empty
+ * string is a valid return value; callers still emit the sentinel <style> element.
+ */
+function flattenStyles(styles: unknown): string {
+  if (styles == null) return '';
+  const list = Array.isArray(styles) ? styles : [styles];
+  return list.filter((s): s is string => typeof s === 'string').join('\n');
+}
+
 /** Regex to find custom element tags (tags containing a hyphen). */
 const CE_TAG_RE = /<([a-z][a-z0-9]*-[a-z0-9-]*)([^>]*)(?:\/>|>([\s\S]*?)<\/\1>)/gi;
 
@@ -172,14 +187,32 @@ function expandNestedCEs(html: string, ceMap: Map<string, Function>, depth = 0):
     if (attrStr && /\bhydrated\b/.test(attrStr)) return match;
     const ComponentClass = ceMap.get(tag);
     if (!ComponentClass) return match; // Not registered — leave as-is.
-    // First expand any nested CEs in the children (bottom-up).
+    // Expand any nested CEs in the children (bottom-up) — used by both branches.
     const expandedChildren = childContent
       ? expandNestedCEs(childContent, ceMap, depth + 1)
       : undefined;
     const attrs = parseAttrs(attrStr || '');
+    const shadow = (ComponentClass as unknown as { shadow?: 'open' | 'closed' }).shadow;
+    if (shadow === 'open' || shadow === 'closed') {
+      // Shadow-DOM component → emit Declarative Shadow DOM and preserve the
+      // host's original light-DOM children so the browser composes <slot>
+      // natively. Do NOT pass children as instance.innerHTML — slot composition
+      // is the contract; reading this.innerHTML in a shadow render() is incoherent.
+      const innerHTML = renderComponent(ComponentClass, attrs, undefined, undefined);
+      const expandedInner = expandNestedCEs(innerHTML, ceMap, depth + 1);
+      const styles = flattenStyles((ComponentClass as unknown as { styles?: unknown }).styles);
+      const dsd =
+        `<template shadowrootmode="${shadow}">` +
+        `<style id="${DSD_SENTINEL_STYLE_ID}">${styles}</style>` +
+        expandedInner +
+        `</template>`;
+      const lightChildren = expandedChildren ?? '';
+      return `<${tag}${attrStr || ''} hydrated>${dsd}${lightChildren}</${tag}>`;
+    }
+    // Light-DOM component → existing behavior: render template replaces children,
+    // original children pass through as instance.innerHTML for wrapper components.
     const innerHTML = renderComponent(ComponentClass, attrs, undefined, expandedChildren);
     if (!innerHTML) return match;
-    // Then expand any nested CEs in this component's rendered output.
     const expanded = expandNestedCEs(innerHTML, ceMap, depth + 1);
     return `<${tag}${attrStr || ''} hydrated>${expanded}</${tag}>`;
   });
@@ -215,9 +248,19 @@ async function* renderElenaPage(
     );
   }
 
+  const shadow = (ComponentClass as unknown as { shadow?: 'open' | 'closed' }).shadow;
   const innerHTML = renderComponent(ComponentClass, {}, serverData);
-  // Expand any nested custom elements in the render output.
   const expanded = expandNestedCEs(innerHTML, ceMap);
+  if (shadow === 'open' || shadow === 'closed') {
+    const styles = flattenStyles((ComponentClass as unknown as { styles?: unknown }).styles);
+    const dsd =
+      `<template shadowrootmode="${shadow}">` +
+      `<style id="${DSD_SENTINEL_STYLE_ID}">${styles}</style>` +
+      expanded +
+      `</template>`;
+    yield `<${tag} hydrated>${dsd}</${tag}>`;
+    return;
+  }
   yield `<${tag} hydrated>${expanded}</${tag}>`;
 }
 
diff --git a/src/adapter/elena/runtime/LitroLink.ts b/src/adapter/elena/runtime/LitroLink.ts
index ae408aa0bbe7ca94e4144a212afb7e153a749787..ff1f845c94841d021fa4fdfe867440ea2df84782 100644
--- a/src/adapter/elena/runtime/LitroLink.ts
+++ b/src/adapter/elena/runtime/LitroLink.ts
@@ -1,46 +1,57 @@
 /**
  * LitroLink (Elena) — <litro-link>
  *
- * Elena implementation of the SPA navigation link. Mirrors the Lit/FAST
- * version's behavior:
+ * Composite SPA-navigation wrapper. Author writes:
  *
- * 1. Light DOM with an inner <a> for progressive enhancement
- * 2. Capture-phase click handler on the host element
- * 3. Intercepts same-origin, modifier-free left clicks on internal paths
- * 4. Dynamic import of litro-router (client-only)
+ *   <litro-link><a href="/path">Label</a></litro-link>
  *
- * Elena uses static props for reactive attributes and render() for the
- * inner <a> element. Since Elena renders to light DOM, the <a> is directly
- * in the document tree — no shadow root.
+ * This element has no render(), no shadow root, no styles, no props.
+ * Children pass through to the parent's tree (the author-supplied <a>
+ * stays in light DOM relative to whatever shadow root encloses it, so
+ * link-hint tools / screen readers / focus traversal see it normally).
+ *
+ * On click, the host's capture-phase handler walks composedPath to find
+ * the inner <a>. If the anchor is an internal same-origin path with no
+ * `target` attribute and the click has no modifier keys, the default is
+ * prevented and the URL is routed via LitroRouter.go(). Otherwise the
+ * click passes through (Cmd-click for new tab, middle-click, external
+ * links, fragments, downloads all behave like a plain <a>).
  */
 
-import { Elena, html } from '@elenajs/core';
+import { Elena } from '@elenajs/core';
 
 export class LitroLink extends Elena(HTMLElement) {
   static tagName = 'litro-link';
-  static props = ['href', 'target', 'rel'];
-
-  href = '';
-  target = '';
-  rel = '';
+  // No shadow, no styles, no props, no render() — Elena's _applyRender
+  // no-ops when render() returns undefined (see the LitroOutlet precedent).
 
   private _clickHandler = (e: MouseEvent): void => {
-    if (this.target) return;
+    if (e.button !== 0) return;
     if (e.metaKey || e.ctrlKey || e.shiftKey || e.altKey) return;
-    if (!this.href.startsWith('/')) return;
+    if (e.defaultPrevented) return;
+
+    const path = e.composedPath();
+    let anchor: HTMLAnchorElement | undefined;
+    for (const node of path) {
+      if (node === this) break;
+      if (node instanceof HTMLAnchorElement) {
+        anchor = node;
+        break;
+      }
+    }
+    if (!anchor) return;
+    if (anchor.target) return;
+    const href = anchor.getAttribute('href');
+    if (!href || !href.startsWith('/')) return;
 
     e.preventDefault();
     void import('@beatzball/litro-router').then(({ LitroRouter }) =>
-      LitroRouter.go(this.href),
+      LitroRouter.go(href),
     );
   };
 
   override connectedCallback(): void {
-    // Register capture-phase click handler BEFORE super.connectedCallback()
-    // so it is active immediately on SSR'd elements (before Elena's
-    // hydration logic runs).
     this.addEventListener('click', this._clickHandler, true);
-
     if (typeof super.connectedCallback === 'function') {
       super.connectedCallback();
     }
@@ -52,17 +63,6 @@ export class LitroLink extends Elena(HTMLElement) {
       super.disconnectedCallback();
     }
   }
-
-  render() {
-    // Elena is light DOM — <slot> has no meaning without shadow DOM.
-    // Use this.text (Elena's built-in property that captures the element's
-    // text content before hydration) to render the link text.
-    return html`<a
-      href="${this.href}"
-      target="${this.target}"
-      rel="${this.rel}"
-    >${this.text}</a>`;
-  }
 }
 
 LitroLink.define();
diff --git a/src/plugins/path-to-route.ts b/src/plugins/path-to-route.ts
index 1eda380860b8900f201db9f07e3a36f785a6bd73..952725e1e0cba6711c0bba32be522b1fe3e243dd 100644
--- a/src/plugins/path-to-route.ts
+++ b/src/plugins/path-to-route.ts
@@ -22,9 +22,15 @@ import type { LitroRoute } from '../types/route.js';
  * Conversion table:
  *   index            → '' (stripped — represents parent route)
  *   [param]          → :param
+ *   pre[param]suf    → pre:paramsuf  (prefix and/or suffix supported)
  *   [...param]       → :param(.*)* (catch-all)
  *   [[param]]        → :param? (optional)
  *   anything else    → unchanged (static segment)
+ *
+ * Prefix/suffix support is required for filename conventions like
+ * `pages/@[handle]/[statusId].ts` (Mastodon-style URLs) where a literal
+ * character precedes the bracketed param within the same segment. Nuxt
+ * and SvelteKit allow the same pattern.
  */
 function transformSegment(segment: string): string {
   // Catch-all: [...param] → :param(.*)* — must be tested before single-bracket
@@ -39,10 +45,14 @@ function transformSegment(segment: string): string {
     return `:${optionalMatch[1]}?`;
   }
 
-  // Dynamic: [param] → :param
-  const dynamicMatch = segment.match(/^\[(.+)\]$/);
+  // Dynamic with optional prefix/suffix: pre[param]suf → pre:paramsuf
+  // Prefix and suffix may be empty, so this also covers the bare [param] case.
+  // The character classes exclude brackets so the optional/catch-all forms
+  // above remain unambiguous.
+  const dynamicMatch = segment.match(/^([^\[\]]*)\[([^\[\]]+)\]([^\[\]]*)$/);
   if (dynamicMatch) {
-    return `:${dynamicMatch[1]}`;
+    const [, prefix, param, suffix] = dynamicMatch;
+    return `${prefix}:${param}${suffix}`;
   }
 
   // index → '' (stripped by the caller)
@@ -54,11 +64,15 @@ function transformSegment(segment: string): string {
 }
 
 /**
- * Returns true if a transformed route segment is dynamic (starts with ':')
- * or is a catch-all (contains '(.*)').
+ * Returns true if a transformed route segment is dynamic — either a bare
+ * `:param` token or a prefixed `pre:param` token (e.g. `@:handle`).
+ *
+ * Detects a colon followed by an identifier character so static segments
+ * that happen to contain an unrelated `:` (none today, but defensive)
+ * aren't misclassified as dynamic.
  */
 function isSegmentDynamic(transformed: string): boolean {
-  return transformed.startsWith(':');
+  return /:[A-Za-z_]/.test(transformed);
 }
 
 /**
