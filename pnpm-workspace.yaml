packages:
  - "apps/*"
  - "packages/*"

# UnoCSS pulls in @unocss/inspector → vue-flow-layout, which declares Vue as
# a peer. We don't use the inspector and don't ship Vue. Suppress the warning
# to keep CI green.
peerDependencyRules:
  ignoreMissing:
    - "vue"
